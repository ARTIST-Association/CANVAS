import "bootstrap/dist/css/bootstrap.min.css";
import "bootstrap-icons/font/bootstrap-icons.css";
import "bootstrap/dist/js/bootstrap.bundle.min.js";

// Entry point for the CANVAS editor. The editor itself is wired up in later
// phases via the DI container in editor/createEditor.ts.
console.info("CANVAS editor bundle loaded");
